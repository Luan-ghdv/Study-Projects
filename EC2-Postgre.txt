CRIAR SENHAS(concluído), PRIVILÉGIOS PARA O USUÁRIO(concluído), PERMITIR CONEXÕES REMOTAS, CONFIGURAR O GRUPO DE SEGURANÇA, CONECTAR REMOTAMENTE

psql -d postgre_aula -U postgres

CREATE ROLE nome_do_usuario WITH LOGIN PASSWORD 'senha';

GRANT CREATE ON DATABASE postgre_aula TO meu_usuario;

sudo -u postgres psql
\c postgre_aula

sudo nano /etc/postgresql/X.X/main/postgresql.conf

